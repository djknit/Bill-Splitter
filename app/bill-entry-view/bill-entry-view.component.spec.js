'use strict';

describe('billEntryView module', function() {

  beforeEach(module('billEntryView'));

  describe('billEntryView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var billEntryViewCtrl = $controller('billEntryViewCtrl');
      expect(billEntryViewCtrl).toBeDefined();
    }));

  });
});